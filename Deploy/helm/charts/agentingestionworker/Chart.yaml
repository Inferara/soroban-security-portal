apiVersion: v2
description: Agent Ingestion Worker — polls internal API endpoints, runs OpenCode, submits results.
type: application
name: agentingestionworker
version: 1.0.0
appVersion: 1.0.0
kubeVersion: ">=1.16.0-0"
