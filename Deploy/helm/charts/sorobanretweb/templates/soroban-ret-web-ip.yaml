apiVersion: v1
kind: Service
metadata:
  namespace: "{{ .Values.global.environment.namespace }}"
  name: "{{ .Values.global.environment.name }}-soroban-ret-web-ip"
spec:
  selector:
    app: "{{ .Values.global.environment.name }}-soroban-ret-web"
  type: ClusterIP
  ports:
    - port: 8787
      targetPort: 8787
