[
  {
    "title": "Fee-on-Transfer Tokens Can Break Intent Accounting",
    "description": "The Intent Bridge records `source_amount` as the canonical escrowed amount but does not verify the actual token amount received by the contract after `transfer()`. Because the contract accepts arbitrary token contract addresses, a non-standard token may transfer less than the requested amount while still succeeding. The bridge then stores the nominal `source_amount`, but later `fill()` and `refund()` attempt to transfer the full recorded amount out, which can fail due to insufficient balance, rendering the affected intent non-functional under the normal settlement/refund flow.\n\n```rust\n// intent_bridge/src/lib.rs - create_intent()\nlet token_client = token::Client::new(&env, &source_token);\ntoken_client.transfer(&sender, &env.current_contract_address(), &source_amount);\n\n// intent_bridge/src/lib.rs - fill()\ntoken_client.transfer(&env.current_contract_address(), &relayer, &intent.source_amount);\n\n// intent_bridge/src/lib.rs - refund()\ntoken_client.transfer(&env.current_contract_address(), &intent.sender, &intent.source_amount);\n```\n\n**Recommendation:** Restrict supported assets through an on-chain allowlist, or verify the actual post-transfer balance delta and store the amount actually received:\n\n```rust\nlet balance_before = token_client.balance(&env.current_contract_address());\ntoken_client.transfer(&sender, &env.current_contract_address(), &source_amount);\nlet balance_after = token_client.balance(&env.current_contract_address());\nlet actual_received = balance_after - balance_before;\n```",
    "severity": "medium",
    "tags": ["token-transfer", "accounting", "escrow", "fee-on-transfer"],
    "category": 0
  },
  {
    "title": "Missing Destination Field Validation Allows Malformed Intents On-Chain",
    "description": "`create_intent()` does not validate several user-supplied destination-side metadata fields. `destination_amount` accepts zero and negative `i128` values (while `source_amount` is validated with `<= 0`). `destination_chain` and `destination_address` accept empty strings with no non-empty check enforced. Malformed destination metadata can be committed on-chain and emitted in events, allowing malformed intents to enter the bridge flow with nonsensical routing or amount information, where they can remain pending until refunded after the deadline.\n\n```rust\nif source_amount <= 0 { return Err(Error::ZeroAmount); }  // validated\nif memo.len() > 28 { return Err(Error::MemoTooLong); }    // validated\n// destination_amount: no validation\n// destination_chain: no validation\n// destination_address: no validation\n```\n\n**Recommendation:** Add input validation for destination-side fields in `create_intent()`:\n\n```rust\nif destination_amount <= 0 {\n    return Err(Error::ZeroAmount);\n}\nif destination_chain.len() == 0 || destination_address.len() == 0 {\n    return Err(Error::EmptyDestination);\n}\n```",
    "severity": "note",
    "tags": ["input-validation", "intent-bridge", "data-integrity"],
    "category": 0
  },
  {
    "title": "Missing Existence Check in remove_memo_mapping Emits Spurious Events",
    "description": "`remove_memo_mapping()` calls `persistent().remove()` without checking whether the mapping exists. Although `Error::MemoNotFound` was defined for this case, it is never used. A `MemoMappingRemovedEvent` is emitted even when nothing was actually removed, introducing spurious event entries.\n\n```rust\n// No existence check:\nenv.storage().persistent().remove(&DataKey::MemoMapping(memo.clone()));\n// Event emitted regardless:\nenv.events().publish((symbol_short!(\"memo_rm\"),), event);\n```\n\n**Recommendation:** Verify the mapping exists before removal:\n\n```rust\nif !env.storage().persistent().has(&DataKey::MemoMapping(memo.clone())) {\n    return Err(Error::MemoNotFound);\n}\n```",
    "severity": "note",
    "tags": ["storage", "events", "forwarder", "memo-mapping"],
    "category": 0
  },
  {
    "title": "Unused Error Variants Reduce ABI Clarity",
    "description": "Several error variants are defined but never returned:\n\n- **Intent Bridge:** `Error::AlreadyInitialized` (1), `Error::InvalidDeadlineDuration` (11). The constructor uses `panic!()` instead of returning these errors.\n- **Token Forwarder:** `Error::MemoNotFound` (5). Defined but unused.\n\n**Recommendation:** Remove unused variants or wire them into the code paths (e.g., use `Error::InvalidDeadlineDuration` in the constructor with a `Result` return type instead of `panic!()`).",
    "severity": "note",
    "tags": ["code-quality", "error-handling", "abi"],
    "category": 0
  },
  {
    "title": "Outdated soroban-sdk Dependency Contains Three Known CVEs",
    "description": "Both contracts pin `soroban-sdk = \"22.0.0\"`, which is affected by three published CVEs:\n\n| CVE | CVSS | Issue | Fixed In |\n|---|---|---|---|\n| CVE-2026-24889 | Medium | `Bytes::slice`, `Vec::slice`, `Prng::gen_range` overflow | >= 22.0.9 |\n| CVE-2026-26267 | High | `#[contractimpl]` resolves inherent over trait impl, bypassing auth | >= 22.0.10 |\n| CVE-2026-32322 | Medium (5.3) | `Fr` scalar field equality bypasses modular reduction | >= 22.0.11 |\n\nThe vulnerable code paths are not exercised in current contracts, but the dependency ships known vulnerable code. Future modifications could inadvertently exercise affected APIs.\n\n**Recommendation:** Upgrade to `soroban-sdk >= 22.0.11` and recompile/redeploy:\n\n```toml\n[dependencies]\nsoroban-sdk = \"22.0.11\"\n```",
    "severity": "note",
    "tags": ["dependencies", "cve", "soroban-sdk", "supply-chain"],
    "category": 0
  },
  {
    "title": "Immutable Role Configuration Prevents On-Chain Key Rotation After Deployment",
    "description": "Both contracts set privileged roles at deployment that cannot be updated post-deployment:\n\n- **Intent Bridge:** `Messenger` and `Relayer` are set once in `__constructor()`. No setter functions exist.\n- **Token Forwarder:** `Admin` is set once in `__constructor()`. While `set_proxy_address()` exists for proxy rotation, the `Admin` key itself is immutable — no `set_admin()` function is implemented.\n- **Intent Bridge:** `deadline_duration` is set once and cannot be tuned post-deployment.\n\n```rust\n// intent_bridge/src/lib.rs: __constructor\nenv.storage().instance().set(&DataKey::Messenger, &messenger);\nenv.storage().instance().set(&DataKey::Relayer, &relayer);\n// No set_messenger() or set_relayer() function exists\n\n// forwarder/src/lib.rs: __constructor\nenv.storage().instance().set(&DataKey::Admin, &admin);\n// set_proxy_address() exists, but no set_admin() function is implemented\n```\n\nIf any privileged key or parameter must change after deployment, a full contract redeployment is required. Pending intents in the old bridge contract remain bound to the old configuration.\n\n**Recommendation:** Evaluate whether post-deployment role rotation is needed. If so, introduce a privileged admin role with setter functions (ideally using a two-step `propose -> accept` transfer pattern). If immutability is intentional, document it explicitly as a design decision and define the expected redeployment procedure.",
    "severity": "note",
    "tags": ["access-control", "key-rotation", "operational", "immutability"],
    "category": 0
  },
  {
    "title": "Missing Persistent TTL Extension on Memo Mappings Can Cause Silent Routing Failure",
    "description": "`set_memo_mapping()` writes memo-to-address mappings to persistent storage but never calls `extend_ttl()`. Unlike the Intent Bridge which extends persistent entry TTL on every `create_intent`/`fill`/`refund`, the Token Forwarder never extends memo mapping TTL. After sufficient inactivity, memo mapping entries age into archival. Memo lookup can fail and the archived mapping becomes inaccessible unless restored, causing G→C wallet routing to break.\n\n```rust\n// forwarder/src/lib.rs: set_memo_mapping()\nenv.storage().persistent().set(&DataKey::MemoMapping(memo.clone()), &destination);\n// No extend_ttl()\n```\n\n**Recommendation:** Add TTL extension on memo mapping writes and reads:\n\n```rust\nlet key = DataKey::MemoMapping(memo.clone());\nenv.storage().persistent().set(&key, &destination);\nenv.storage().persistent().extend_ttl(&key, MEMO_TTL_THRESHOLD, MEMO_TTL_EXTEND);\n```\n\nExtend TTL on reads in `get_memo_destination()` as well, to ensure active mappings remain live.",
    "severity": "low",
    "tags": ["ttl", "storage", "forwarder", "memo-mapping", "soroban"],
    "category": 0
  },
  {
    "title": "Missing Instance Storage TTL Extension Can Cause Temporary Contract Unavailability",
    "description": "Neither contract calls `env.storage().instance().extend_ttl()`. Instance storage holds critical configuration values:\n\n- **Intent Bridge:** `Messenger`, `Relayer`, `DeadlineDuration`\n- **Token Forwarder:** `Admin`, `ProxyAddress`\n\nOn Soroban, instance storage is a persistent ledger entry subject to TTL expiration. When TTL reaches zero, the contract instance is archived at the protocol level — normal invocations fail unless archival is handled via a restore-aware transaction flow.\n\n```rust\n// intent_bridge/src/lib.rs: __constructor\nenv.storage().instance().set(&DataKey::Messenger, &messenger);\nenv.storage().instance().set(&DataKey::Relayer, &relayer);\nenv.storage().instance().set(&DataKey::DeadlineDuration, &deadline_duration);\n// No env.storage().instance().extend_ttl() anywhere in the contract\n```\n\nIf the Intent Bridge instance is archived while intents are in `PENDING` status: `fill()` fails (messenger cannot confirm cross-chain completion), `refund()` fails (sender cannot reclaim funds after deadline), and funds remain temporarily inaccessible until archival is handled through Soroban's restore flow. Instance storage is restorable via `RestoreFootprintOp` (no permanent fund loss).\n\n**Recommendation:** Add instance TTL extension to functions that are expected to keep the contract operational:\n\n```rust\nconst INSTANCE_TTL_THRESHOLD: u32 = 120960; // 7 days\nconst INSTANCE_TTL_EXTEND: u32 = 241920;    // 14 days\n\nenv.storage().instance().extend_ttl(INSTANCE_TTL_THRESHOLD, INSTANCE_TTL_EXTEND);\n```",
    "severity": "low",
    "tags": ["ttl", "storage", "instance-storage", "soroban", "availability"],
    "category": 0
  }
]
