/**
 * Theme exports barrel file.
 *
 * Import from 'theme' for all theme constants and responsive utilities:
 * ```tsx
 * import { AccentColors, Layout, formControlWidth } from '../../theme';
 * ```
 *
 * For severity colors, continue to import from ThemeContext:
 * ```tsx
 * import { SeverityColors } from '../../contexts/ThemeContext';
 * ```
 */
export * from './constants';
export * from './responsive';
